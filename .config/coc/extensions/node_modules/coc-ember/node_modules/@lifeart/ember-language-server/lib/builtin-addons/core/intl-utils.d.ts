import { Location } from 'vscode-languageserver';
import { Server } from '../..';
declare type Translations = {
    locale: string;
    text: string;
    location?: Location;
};
declare type TranslationsHashMap = Record<string, Translations[]>;
export declare function getTranslations(root: string, server: Server): Promise<TranslationsHashMap>;
export {};
