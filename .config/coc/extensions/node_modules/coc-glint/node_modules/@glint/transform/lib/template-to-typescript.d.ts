import { RewriteResult } from './map-template-contents';
import { GlintEmitMetadata } from '@glint/config/src/environment';
export declare type TemplateToTypescriptOptions = {
    typesPath: string;
    meta?: GlintEmitMetadata | undefined;
    globals?: Array<string> | undefined;
    contextType?: string;
    typeParams?: string;
    preamble?: Array<string>;
    useJsDoc?: boolean;
};
/**
 * Given the text contents of a template, returns a  TypeScript representation
 * of that template's contents, as well as a mapping of offsets and ranges between
 * the original and transformed contents.
 */
export declare function templateToTypescript(template: string, { typesPath, globals, meta, typeParams, contextType, preamble, useJsDoc, }: TemplateToTypescriptOptions): RewriteResult;
